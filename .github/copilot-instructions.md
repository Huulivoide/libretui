# Copilot Instructions

## Project

`libretui` is a Node.js project using **ESM** (`import`/`export` syntax). All source files must use ES module syntax — no `require()`.

## Package Manager

Use **npm**.

## Commands

```bash
npm test        # Run tests
```

> This file should be updated as the project grows (build steps, lint, test runner, architecture).
